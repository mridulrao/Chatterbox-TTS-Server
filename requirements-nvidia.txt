# requirements-nvidia.txt (For NVIDIA GPU Installation)
#
# NOTE:
# chatterbox-tts currently pins torch==2.6.0, so we must align torch/vision/audio.
# PyTorch 2.6.0 wheels are available for cu118/cu124/cu126 (NOT cu121).  :contentReference[oaicite:1]{index=1}

# ------------------------------------------------------------
# PyTorch CUDA wheel index (use cu124 for torch 2.6.0)
# ------------------------------------------------------------
--index-url https://download.pytorch.org/whl/cu124

# ------------------------------------------------------------
# Torch stack (aligned with chatterbox-tts expectations)
# ------------------------------------------------------------
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# ------------------------------------------------------------
# CRITICAL dependency pins (prevents ONNX source build)
# ------------------------------------------------------------
numpy>=1.24.0,<2.0
onnx==1.19.1
s3tokenizer==0.2.0

# ------------------------------------------------------------
# Chatterbox TTS engine (your fork)
# ------------------------------------------------------------
chatterbox-tts @ git+https://github.com/mridulrao/chatterbox.git@master

# ------------------------------------------------------------
# Core Web Framework
# ------------------------------------------------------------
fastapi
uvicorn[standard]

# ------------------------------------------------------------
# ML & Audio Libraries
# ------------------------------------------------------------
soundfile                 # Requires libsndfile system library
librosa
safetensors
descript-audio-codec

# ------------------------------------------------------------
# Configuration & Utilities
# ------------------------------------------------------------
PyYAML
python-multipart
requests
Jinja2
watchdog
aiofiles
unidecode
inflect
tqdm
hf_transfer               # Speed up HF downloads

# ------------------------------------------------------------
# Audio Post-processing
# ------------------------------------------------------------
pydub
audiotsm
praat-parselmouth
