# requirements-nvidia.txt (For NVIDIA GPU Installation with CUDA 12.1)
# This file ensures a compatible set of CUDA-enabled torch packages are installed.

# ------------------------------------------------------------
# PyTorch CUDA 12.1 index
# ------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cu121

# ------------------------------------------------------------
# Torch stack (CUDA 12.1)
# ------------------------------------------------------------
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# ------------------------------------------------------------
# CRITICAL dependency pins (prevents ONNX source build)
# ------------------------------------------------------------
# Python 3.11 compatible wheels only
numpy>=1.24.0,<2.0
onnx==1.19.1

# s3tokenizer pulls ONNX transitively – pin it explicitly
s3tokenizer==0.2.0

# ------------------------------------------------------------
# Chatterbox TTS engine (your fork)
# ------------------------------------------------------------
chatterbox-tts @ git+https://github.com/mridulrao/chatterbox.git@master

# ------------------------------------------------------------
# Core Web Framework
# ------------------------------------------------------------
fastapi
uvicorn[standard]

# ------------------------------------------------------------
# ML & Audio Libraries
# ------------------------------------------------------------
soundfile                 # Requires libsndfile system library
librosa
safetensors
descript-audio-codec

# ------------------------------------------------------------
# Configuration & Utilities
# ------------------------------------------------------------
PyYAML
python-multipart
requests
Jinja2
watchdog
aiofiles
unidecode
inflect
tqdm
hf_transfer               # Speed up HF downloads

# ------------------------------------------------------------
# Audio Post-processing
# ------------------------------------------------------------
pydub
audiotsm
praat-parselmouth
